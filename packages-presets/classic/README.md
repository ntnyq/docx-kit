# @docxkit/preset-classic

Classic style preset for docx-kit — government document style with SimHei.

## Usage

```ts
import { classicPreset } from '@docxkit/preset-classic'

const docx = createDocx({ ...classicPreset.config })
```
