# @docxkit/preset-modern

Modern style preset for docx-kit — business document style with Calibri.

## Usage

```ts
import { modernPreset } from '@docxkit/preset-modern'

const docx = createDocx({ ...modernPreset.config })
```
