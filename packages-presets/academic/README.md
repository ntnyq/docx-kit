# @docxkit/preset-academic

Academic style preset for docx-kit — thesis/dissertation formatting with Times New Roman.

## Usage

```ts
import { academicPreset } from '@docxkit/preset-academic'

const docx = createDocx({ ...academicPreset.config })
```
