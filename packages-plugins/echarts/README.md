# @docxkit/plugin-echarts

ECharts chart embedding plugin for docx-kit (browser only).

## Usage

```ts
import { echartsPlugin } from '@docxkit/plugin-echarts'

builder.use(echartsPlugin)
builder.echarts({ option: { ... } })
```
