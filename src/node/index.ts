/**
 * Node.js platform barrel — re-exports all Node.js–specific APIs.
 *
 * @module node
 */

// Filesystem
export { saveDocument } from './fs'

// Base64 decoding
export { dataUrlToUint8Array } from './dataUrl'
