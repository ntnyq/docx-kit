# @docxkit/create-plugin

Scaffolding CLI for creating new docx-kit plugins.

## Usage

```bash
npx @docxkit/create-plugin my-plugin
```

This will generate a new plugin project with the proper structure, build config, and test setup.
