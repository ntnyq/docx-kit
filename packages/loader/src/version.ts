/**
 * docx-kit version used by platform loader compatibility checks.
 */
import packageJson from '../package.json'

/**
 * Host compatibility version for this lockstep-released loader package.
 */
export const DOCX_KIT_VERSION = packageJson.version
